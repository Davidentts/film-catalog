name: Python checks 🐍

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder GH Actions
        run: echo "Hello Checks!"
